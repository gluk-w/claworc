---
/**
 * Per-page <Footer /> slot is intentionally empty.
 *
 * The site-wide marketing Footer renders once at the page level via the
 * PageFrame override in src/components/docs/PageFrame.astro, so it spans
 * the full viewport below Starlight's sidebar+content layout (matching
 * /blog and the marketing pages).
 */
---
