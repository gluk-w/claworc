#!/bin/sh
# restart — restart the agent service (docs/shim.md). The template has no
# long-running agent daemon (CHAT_CMD is spawned per turn), so this is a
# no-op success. If you add an s6 service for your agent (conventionally
# `svc-agent`), replace this with:
#   exec /command/s6-svc -r /run/service/svc-agent
exit 0
